# Visual-approved render package

`VisualApprovedRenderPackage` is the final production gate joining reviewed design intent, resolved render, assets, canonical artifacts and pixel QA summary. Canonical SVG/checksum stay authoritative; server materialization and PNG raster are ephemeral derivatives. A ready RenderSession with generated media is not final until pixel approval succeeds.
