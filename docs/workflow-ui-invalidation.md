# Workflow UI invalidation

Draft changes mark dependent results stale without deleting the previous output. Copy or tone invalidates creative direction and downstream stages; brand changes begin at brand intelligence; reference changes begin at reference intelligence; format changes begin at layout. Existing service fingerprints remain the final cache and reuse authority.

The timeline reports concrete stages rather than fabricated percentages. An approved badge requires a valid production authority, never merely the existence of a render.
