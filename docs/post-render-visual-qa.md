# Post-render visual QA

Pre-render approval approves intent and structure, not pixels. Phase 10 first runs a zero-cost local audit, then reviews generated assets and the rasterized composite. Mechanical blockers stop before AI. Generated projects require `visualApproved`; deterministic-only projects may be locally approved.
