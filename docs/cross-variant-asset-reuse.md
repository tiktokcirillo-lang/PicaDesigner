# Cross-variant asset reuse

Family planning groups requirements before generation. Resolution order is official/source reuse, existing generated reuse, shared generation with responsive crop, then format-specific generation when focal viability fails. `visual_reference` remains outside the render registry. Official logo and uploaded product identity are checksum-preserved. The dependency graph records every asset’s dependent variants so a shared correction rerenders only those variants.
