# Campaign family authority

Single-format `ProductionAuthorityRecord` remains unchanged. Each approved variant still owns one such authority. Only after four valid authorities may the immutable `CampaignFamilyAuthority` reference their IDs and the invariant fingerprint. The browser cannot submit packages or authority payloads. Neon migration 004 adds family, variant and family-authority records plus a nullable project pointer.
