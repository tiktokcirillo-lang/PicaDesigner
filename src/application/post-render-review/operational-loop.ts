import {createHash} from 'node:crypto';import {buildAssetRegenerationDirectives,buildRegenerationPromptPlan,createUpstreamDirectives,POST_RENDER_REVIEW_SCHEMA_VERSION,resolvePostRenderDecision,type AssetRegenerationRound,type PostRenderReviewSession,type PostRenderVisualQaProvider,type VisualQaTarget} from '../../domain/post-render-review/index.js';import {canonicalImageJobInput,validateGeneratedImage,type GeneratedAssetStore,type GeneratedImageAsset,type ImageGenerationProvider,type ImageRegenerationJob} from '../../domain/image-assets/index.js';import {createRenderSession} from '../render-engine/index.js';import {materializeSvgAssetsForServer} from '../../infrastructure/post-render-review/index.js';import type {CompositeRasterizer} from '../../domain/post-render-review/index.js';import type {ProjectAIBudgetCoordinator} from '../../infrastructure/ai/index.js';import type {AIModelCall} from '../../infrastructure/ai/types.js';
const sha=(v:string|Uint8Array)=>createHash('sha256').update(v).digest('hex');
export const executeVisualCorrectionLoop=async(initial:PostRenderReviewSession,input:{reviewedDesignPackage:any;imageAssetSession:any;assetRegistry:any},d:{store:GeneratedAssetStore;imageProvider:ImageGenerationProvider;qaProvider:PostRenderVisualQaProvider;rasterizer:CompositeRasterizer;budget:ProjectAIBudgetCoordinator;imageModel:string;regenLimitUsd:number;verificationReserveUsd:number;maxRounds:number;maxMb:number;storageRetries:number}):Promise<PostRenderReviewSession>=>{const review=initial.finalReview;if(!review||resolvePostRenderDecision(review)!=='regeneration_required'||d.maxRounds<1)return{...initial,upstreamDirectives:review?createUpstreamDirectives(review.issues):[]};const directives=buildAssetRegenerationDirectives(review,input.imageAssetSession.generatedAssets,input.imageAssetSession.jobs,Boolean(d.imageProvider.edit));if(!directives.length)return{...initial,outcome:'upstream_revision_required',upstreamDirectives:[],warnings:[...initial.warnings,'No safe automatic correction mapping.']};const estimate=directives.length*d.imageProvider.estimateCost(input.imageAssetSession.jobs[0]),snapshot=await d.budget.snapshot(initial.projectId);if(estimate+d.verificationReserveUsd>Math.min(snapshot.remainingUsd,snapshot.monthlyRemainingUsd,d.regenLimitUsd)){return{...initial,outcome:'failed',warnings:[...initial.warnings,'O limite de geração deste projeto foi atingido antes da correção visual.']}}const sourceRaster=initial.qaPlan.compositeTarget?.checksum??'',round:AssetRegenerationRound={round:1,sourceRenderSessionId:input.imageAssetSession.renderSession.sessionId,sourceRasterChecksum:sourceRaster,sourceAssetIds:directives.map(x=>x.currentAssetId),sourceAssetChecksums:directives.map(x=>input.imageAssetSession.generatedAssets.find((a:any)=>a.id===x.currentAssetId)?.checksum??''),issueIds:directives.flatMap(x=>x.issueIds),directives,operationResults:[],newAssetIds:[],newAssetChecksums:[],cost:0,status:'generating'},newAssets:GeneratedImageAsset[]=[];for(const directive of directives){const source=input.imageAssetSession.generatedAssets.find((a:any)=>a.id===directive.currentAssetId) as GeneratedImageAsset,original=input.imageAssetSession.jobs.find((j:any)=>j.requirementId===directive.requirementId),promptPlan=buildRegenerationPromptPlan(original.promptPlan,directive),fingerprint=sha(canonicalImageJobInput({source:source.checksum,requirement:directive.requirementId,promptPlan,issues:directive.issueIds,corrections:directive.corrections,operation:directive.operation,model:d.imageModel,quality:directive.requestedQuality,size:original.size,policy:POST_RENDER_REVIEW_SCHEMA_VERSION})),job:ImageRegenerationJob={...original,jobId:`regen_${fingerprint.slice(0,16)}`,fingerprint,sourceAssetId:source.id,sourceAssetRefs:[source.backingRef],operation:directive.operation,corrections:directive.corrections,promptPlan,model:d.imageModel},cost=d.imageProvider.estimateCost(job),reservation=await d.budget.reserve({projectId:initial.projectId,stage:'image_regeneration',estimatedCostUsd:cost,stageLimitUsd:d.regenLimitUsd,operationId:fingerprint});let result;try{const sourceBytes=await d.store.get(source.backingRef,initial.projectId);if(job.operation==='edit_if_supported'&&d.imageProvider.edit&&sourceBytes)result=await d.imageProvider.edit({job,sourceBytes,sourceMediaType:source.mediaType});else result=await d.imageProvider.generate(job)}catch(e){if(e instanceof Error&&e.message.toLowerCase().includes('timeout')){await d.budget.resolveUnknown(reservation.reservationId);round.operationResults.push({jobId:job.jobId,status:'unknown',reason:'provider_timeout_unknown'})}else{await d.budget.release(reservation.reservationId);round.operationResults.push({jobId:job.jobId,status:'failed',reason:'provider_rejected'})}continue}const actual=result.actualCostUsd??cost,call:AIModelCall={requestId:result.requestId,pass:'image_generation',model:result.model,inputTokens:result.usage?.inputTokens??0,cachedInputTokens:0,cacheWriteTokens:0,outputTokens:result.usage?.outputTokens??0,costUsd:actual,durationMs:result.durationMs,maxOutputTokens:0,outputTokenUtilization:0,repairAttempt:false,stage:'image_regeneration',operationId:fingerprint};await d.budget.commit(reservation.reservationId,call);round.cost+=actual;try{validateGeneratedImage(result,d.maxMb)}catch{round.operationResults.push({jobId:job.jobId,status:'failed',reason:'generated_output_invalid'});continue}const checksum=sha(result.bytes),id=`regenerated_${checksum.slice(0,20)}`;let backingRef;for(let i=0;i<=d.storageRetries;i++){try{backingRef=await d.store.put({projectId:initial.projectId,assetId:id,bytes:result.bytes,mediaType:result.mediaType,checksum});break}catch{}}if(!backingRef){round.operationResults.push({jobId:job.jobId,status:'failed',reason:'generated_asset_storage_failed'});continue}const asset:GeneratedImageAsset={...source,id,backingRef,checksum,fingerprint,requirementId:directive.requirementId,model:result.model,createdAt:new Date().toISOString(),provenance:[...source.provenance,`supersedes:${source.id}`]};newAssets.push(asset);round.newAssetIds.push(id);round.newAssetChecksums.push(checksum);round.operationResults.push({jobId:job.jobId,status:'succeeded',assetId:id})}if(newAssets.length!==directives.length){round.status='failed';round.failureReason='mandatory_regeneration_incomplete';return{...initial,regenerationRounds:[round],outcome:'failed',visualApproved:false,cost:initial.cost+round.cost,costBreakdown:{initialQaUsd:initial.cost,escalationUsd:0,regenerationUsd:round.cost,verificationUsd:0,totalUsd:initial.cost+round.cost}}}round.status='generated';const registry={projectId:initial.projectId,assets:[...input.assetRegistry.assets,...newAssets]},render=createRenderSession({projectId:initial.projectId,reviewedDesignPackage:input.reviewedDesignPackage,assetRegistry:registry,fontAvailability:['Arial','Helvetica','sans-serif']});if(render.renderDocument.scenes[0]?.source.layoutId!==input.imageAssetSession.renderSession.renderDocument.scenes[0]?.source.layoutId||render.renderDocument.scenes[0]?.source.routeId!==input.imageAssetSession.renderSession.renderDocument.scenes[0]?.source.routeId||render.artifacts[0]?.width!==input.imageAssetSession.renderSession.artifacts[0]?.width)throw new Error('Regeneration changed protected design invariants.');round.resultingRenderSessionId=render.sessionId;round.status='rerendered';const artifact=render.artifacts[0]!,svg=await materializeSvgAssetsForServer(artifact,registry,d.store),raster=await d.rasterizer.rasterize({svg,width:artifact.width,height:artifact.height,sceneId:render.renderDocument.scenes[0]!.sceneId,canonicalSvgChecksum:artifact.checksum,assetChecksums:newAssets.map(a=>a.checksum)});round.resultingRasterChecksum=raster.checksum;const targets:VisualQaTarget[]=[...newAssets.map(a=>({id:`asset:${a.id}`,type:'generated_asset' as const,sourceRef:a.id,width:a.width,height:a.height,checksum:a.checksum,bytes:new Uint8Array(),context:{correction:true},observability:['correction_verification']})),{id:`composite:${raster.artifactId}`,type:'composite',sourceRef:raster.artifactId,width:raster.width,height:raster.height,checksum:raster.checksum,bytes:raster.bytes,context:{verification:true},observability:['integration']}],verified=await d.qaProvider.review(targets,{projectId:initial.projectId,originalIssues:round.issueIds,compactVerification:true}),decision=resolvePostRenderDecision(verified),approved=decision==='approved'||decision==='approved_with_notes';round.verification={issueResults:round.issueIds.map(id=>({issueId:id,resolved:approved})),issuesResolved:approved?round.issueIds:[],issuesRemaining:approved?[]:round.issueIds,newIssues:verified.issues.filter(i=>!round.issueIds.includes(i.id)),overallScore:verified.overallScore,approved,confidence:verified.confidence};round.status=approved?'resolved':'failed';const total=initial.cost+round.cost+d.verificationReserveUsd;return{...initial,regenerationRounds:[round],finalReview:verified,finalRenderSession:render,finalAssetRegistry:registry,outcome:approved?'regenerated_approved':decision==='upstream_revision_required'?'upstream_revision_required':'failed',visualApproved:approved,cost:total,costBreakdown:{initialQaUsd:initial.cost,escalationUsd:0,regenerationUsd:round.cost,verificationUsd:d.verificationReserveUsd,totalUsd:total},upstreamDirectives:createUpstreamDirectives(verified.issues),approvedPackage:approved?{projectId:initial.projectId,reviewSessionId:input.reviewedDesignPackage.reviewSessionId,postRenderReviewSessionId:initial.sessionId,reviewedDesignPackage:input.reviewedDesignPackage,renderSession:render,generatedAssetSession:input.imageAssetSession,finalAssets:registry,canonicalArtifacts:render.artifacts,pixelQaSummary:{score:verified.overallScore,verdict:decision},visualApprovalStatus:'approved',provenance:['controlled_asset_regeneration','compact_visual_verification']}:undefined}};
