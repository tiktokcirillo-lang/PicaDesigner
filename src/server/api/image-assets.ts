import {Router} from 'express';import {generateRequiredAssets,type GenerateRequiredAssetsRequest} from '../../application/image-assets/index.js';import {ProjectAIBudgetCoordinator} from '../../infrastructure/ai/budget/project-coordinator.js';import {applicationBudgetStore} from '../../infrastructure/ai/budget/runtime-store.js';import {createOpenAIClient} from '../../infrastructure/ai/providers/openai/client.js';import {loadOpenAIConfig} from '../../infrastructure/ai/providers/openai/config.js';import {applicationGeneratedAssetStore,MockImageGenerationProvider,OpenAIImageGenerationProvider} from '../../infrastructure/image-generation/index.js';
export const createImageAssetsRouter=()=>{const router=Router();router.post('/generate-required',async(req,res)=>{try{if('prompt'in(req.body??{}))return res.status(400).json({error:'Raw image prompts are not accepted.'});const config=loadOpenAIConfig(),mock=(process.env.AI_IMAGE_PROVIDER??'openai')==='mock',provider=mock?new MockImageGenerationProvider():new OpenAIImageGenerationProvider(createOpenAIClient({...config,requestTimeoutMs:config.imageRequestTimeoutMs,maxRetries:config.imageMaxRetries}),config.imageEstimatedCostUsd),budget=new ProjectAIBudgetCoordinator(applicationBudgetStore,{projectLimitUsd:config.maxProjectCostUsd,monthlyLimitUsd:config.monthlyBudgetUsd,safetyFactor:config.budgetReservationSafetyFactor,ttlSeconds:config.budgetReservationTtlSeconds});const result=await generateRequiredAssets(req.body as GenerateRequiredAssetsRequest,{provider,store:applicationGeneratedAssetStore,budget,model:mock?'mock-image':config.imageModel,targetUsd:config.imageGenerationTargetUsd,limitUsd:config.imageGenerationMaxUsd,maxMb:config.generatedImageMaxMb});return res.json(result)}catch(e){return res.status(422).json({error:e instanceof Error?e.message:'Image asset generation failed.'})}});return router};
