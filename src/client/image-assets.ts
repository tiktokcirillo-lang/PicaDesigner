import type {GenerateRequiredAssetsRequest} from '../application/image-assets/index.js';import type {ImageAssetSession} from '../domain/image-assets/index.js';
export const ensureImageAssets=async(input:GenerateRequiredAssetsRequest):Promise<ImageAssetSession>=>{const response=await fetch('/api/assets/generate-required',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(input)}),type=response.headers.get('content-type')??'';if(!type.includes('application/json')){await response.text();throw new Error(`HTTP ${response.status}: resposta inesperada da infraestrutura.`)}const payload=await response.json() as ImageAssetSession&{error?:string};if(!response.ok)throw new Error(payload.error??`Falha HTTP ${response.status}`);return payload};
