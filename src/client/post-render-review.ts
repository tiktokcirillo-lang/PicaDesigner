import type {EnsurePostRenderReviewRequest} from '../application/post-render-review/index.js';import type {PostRenderReviewSession} from '../domain/post-render-review/index.js';
export const ensurePostRenderReview=async(input:EnsurePostRenderReviewRequest):Promise<PostRenderReviewSession>=>{const response=await fetch('/api/visual-qa/review',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(input)}),type=response.headers.get('content-type')??'';if(!type.includes('application/json')){await response.text();throw new Error(`HTTP ${response.status}: resposta inesperada.`)}const body=await response.json() as PostRenderReviewSession&{error?:string};if(!response.ok)throw new Error(body.error??'Visual QA failed.');return body};
