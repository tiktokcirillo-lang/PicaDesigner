import {useEffect,useRef,useState,type ChangeEvent} from "react";import {FileImage,UploadCloud,X} from "lucide-react";import type {ProjectSourceAssetRole,ProjectSourceAssetSummary} from "../../domain/source-assets/index.js";import {getProjectSourceAssetReadHandle,uploadProjectSourceAsset} from "../../client/source-assets.js";import {IconButton} from "../primitives/Button.js";
const stateLabel={idle:"",preparing:"Preparando upload",uploading:"Enviando",validating:"Validando",failed:"Falhou"} as const;
export function SourceAssetField({projectId,role,label,value,onChange,onError}:{projectId:string;role:ProjectSourceAssetRole;label:string;value?:ProjectSourceAssetSummary;onChange:(asset:ProjectSourceAssetSummary|undefined)=>void;onError:(message:string)=>void}){const[state,setState]=useState<keyof typeof stateLabel>("idle"),[preview,setPreview]=useState(""),localUrl=useRef("");useEffect(()=>{const controller=new AbortController();if(!value){setPreview("");return()=>controller.abort()}getProjectSourceAssetReadHandle(projectId,value.assetId,controller.signal).then(handle=>setPreview(handle.url)).catch(()=>setPreview(""));return()=>controller.abort()},[projectId,value?.assetId]);useEffect(()=>()=>{if(localUrl.current)URL.revokeObjectURL(localUrl.current)},[]);async function select(event:ChangeEvent<HTMLInputElement>){const file=event.target.files?.[0];if(!file)return;if(localUrl.current)URL.revokeObjectURL(localUrl.current);localUrl.current=URL.createObjectURL(file);setPreview(localUrl.current);try{const asset=await uploadProjectSourceAsset({projectId,role,file,supersedesAssetId:value?.assetId,onProgress:setState});onChange(asset);setState("idle")}catch{setState("failed");onError("Não foi possível validar esta imagem.")}}return <div className="source-field"><span className="field__label">{label}</span>{value?<div className="file-ready">{preview?<img src={preview} alt=""/>:<FileImage/>}<div><strong>{value.filename}</strong><span>{value.width}×{value.height} · {(value.byteSize/1024/1024).toFixed(1)} MB · {value.status==="available"?"Disponível":"Indisponível"}</span></div><IconButton label={`Remover ${label}`} onClick={()=>onChange(undefined)}><X size={16}/></IconButton></div>:<label className="dropzone"><UploadCloud size={22}/><strong>{label}</strong><span>JPEG, PNG ou WebP · até 20 MB</span><input aria-label={label} type="file" accept="image/jpeg,image/png,image/webp" disabled={state!=="idle"&&state!=="failed"} onChange={select}/></label>}{stateLabel[state]?<span className={`upload-state upload-state--${state}`}>{stateLabel[state]}</span>:null}</div>}
