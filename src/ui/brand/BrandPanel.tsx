import type {ProjectSourceAssetSummary} from "../../domain/source-assets/index.js";import type {WorkspaceDraft} from "../state/workspace-types.js";import {Field,Input,Switch,Textarea} from "../primitives/Field.js";import {SourceAssetField} from "../source-assets/SourceAssetField.js";
const append=(list:ProjectSourceAssetSummary[],asset:ProjectSourceAssetSummary)=>[...list.filter(item=>item.assetId!==asset.assetId),asset];
export function BrandPanel({projectId,draft,onChange,onError}:{projectId:string;draft:WorkspaceDraft;onChange:(patch:Partial<WorkspaceDraft>)=>void;onError:(message:string)=>void}){return <div className="panel-form"><Switch label="Usar identidade existente" checked={draft.brandEnabled} onChange={brandEnabled=>onChange({brandEnabled})}/>{draft.brandEnabled?<><SourceAssetField projectId={projectId} role="official_logo" label="Logo oficial" value={draft.logoAsset} onChange={logoAsset=>onChange({logoAsset})} onError={onError}/><SourceAssetField projectId={projectId} role="product_image" label="Adicionar imagem de produto" onChange={asset=>asset&&onChange({productAssets:append(draft.productAssets,asset)})} onError={onError}/>{draft.productAssets.map(asset=><SourceAssetField key={asset.assetId} projectId={projectId} role="product_image" label="Imagem de produto" value={asset} onChange={next=>onChange({productAssets:next?append(draft.productAssets,next):draft.productAssets.filter(x=>x.assetId!==asset.assetId)})} onError={onError}/>)}<details><summary>Mais assets oficiais</summary><SourceAssetField projectId={projectId} role="brand_photo" label="Fotografia da marca" onChange={asset=>asset&&onChange({brandPhotoAssets:append(draft.brandPhotoAssets,asset)})} onError={onError}/><SourceAssetField projectId={projectId} role="graphic_asset" label="Asset gráfico" onChange={asset=>asset&&onChange({graphicAssets:append(draft.graphicAssets,asset)})} onError={onError}/></details><div className="color-fields"><Field label="Cor primária"><Input type="color" value={draft.primaryColor} onChange={e=>onChange({primaryColor:e.target.value})}/></Field><Field label="Cor secundária"><Input type="color" value={draft.secondaryColor} onChange={e=>onChange({secondaryColor:e.target.value})}/></Field></div><Field label="Fonte de títulos"><Input value={draft.titleFont} onChange={e=>onChange({titleFont:e.target.value})}/></Field><Field label="Fonte de texto"><Input value={draft.bodyFont} onChange={e=>onChange({bodyFont:e.target.value})}/></Field><Field label="URL da marca"><Input type="url" value={draft.brandUrl} onChange={e=>onChange({brandUrl:e.target.value})}/></Field><Field label="Notas visuais"><Textarea rows={4} value={draft.visualNotes} onChange={e=>onChange({visualNotes:e.target.value})}/></Field></>:<p className="muted-copy">O projeto usará uma identidade provisória, sem inventar uma biblioteca de marca.</p>}</div>}
