import type {ImageEditRequest,ImageGenerationJob,ImageGenerationProvider} from '../../domain/image-assets/index.js';
const png=(salt=0)=>Uint8Array.from([137,80,78,71,13,10,26,10,0,0,0,13,salt]);
export class MockImageGenerationProvider implements ImageGenerationProvider{readonly id='mock';constructor(private behavior:'success'|'edit_failure'|'timeout'='success'){}capabilities(){return{models:['mock-image'],formats:['png' as const],qualities:['draft' as const,'standard' as const,'final' as const],transparentBackground:true,arbitrarySize:true,minEdge:16,maxEdge:3840,edgeMultiple:16,minAspectRatio:1/3,maxAspectRatio:3,edit:true}}estimateCost(){return .01}async generate(j:ImageGenerationJob){if(this.behavior==='timeout')throw new Error('timeout');return{bytes:png(j.fingerprint.charCodeAt(0)%255),mediaType:'image/png' as const,width:j.size.width,height:j.size.height,model:j.model,requestId:`mock_${j.fingerprint.slice(0,12)}`,durationMs:1,actualCostUsd:.01}}async edit(r:ImageEditRequest){if(this.behavior==='edit_failure')throw new Error('edit failed');return{bytes:png((r.sourceBytes.at(-1)??0)+1),mediaType:'image/png' as const,width:r.job.size.width,height:r.job.size.height,model:r.job.model,requestId:`mock_edit_${r.job.fingerprint.slice(0,10)}`,durationMs:1,actualCostUsd:.01}}}
