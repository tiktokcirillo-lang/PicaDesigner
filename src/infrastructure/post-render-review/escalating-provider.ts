import {resolveEscalatedVisualDecision,shouldEscalateVisualQa,type PostRenderVisualQaProvider,type VisualQaTarget} from '../../domain/post-render-review/index.js';
export class EscalatingPostRenderVisualQaProvider implements PostRenderVisualQaProvider{readonly id='qa-router';lastEscalation={executed:false,reason:undefined as string|undefined};constructor(private primary:PostRenderVisualQaProvider,private escalation:PostRenderVisualQaProvider,private enabled=true){}async review(targets:VisualQaTarget[],context:Record<string,unknown>){const terra=await this.primary.review(targets,context);if(!this.enabled||!shouldEscalateVisualQa(terra))return terra;this.lastEscalation={executed:true,reason:'low_confidence_borderline_or_ambiguous'};const sol=await this.escalation.review(targets,{...context,role:'ambiguity_auditor_no_design_mutation',terraReview:terra});return resolveEscalatedVisualDecision(terra,sol)}}
