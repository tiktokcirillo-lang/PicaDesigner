import {strict as assert} from 'node:assert';import {APPROVAL_POLICY,ART_DIRECTOR_REVIEW_SCHEMA_VERSION,MockSeniorArtDirectorCritic,OBSERVABILITY,observabilityGuard,resolveReviewDecision,validateSeniorCritique,type LocalArtDirectorAudit} from './index.js';
const keys=['communicationIntegrity','brandIntegrity','referenceTransformation','creativeConceptIntegrity','creativeDeviceIntegrity','hierarchy','composition','typography','spacingRhythm','negativeSpace','alignment','grouping','visualBalance','contentCompleteness','safeArea','collisionSafety','textFit','formatSuitability','productionFeasibility','antiCliche','antiAiPlanningDiscipline'];const audit=(passed=true):LocalArtDirectorAudit=>({dimensions:Object.fromEntries(keys.map(k=>[k,passed?.95:0])) as LocalArtDirectorAudit['dimensions'],score:passed?.95:0,blockers:passed?[]:[{id:'block',severity:'blocker',domain:'layout',diagnosis:'hard collision',whyItMatters:'unsafe',evidenceRefs:[],recommendedAction:'resolve',revisionTarget:'layout',confidence:1,observability:'observable_pre_render'}],warnings:[],passed});
const critic=new MockSeniorArtDirectorCritic(),good=await critic.critique({} as never);assert.equal(resolveReviewDecision(audit(),good),'approved','G');assert.equal(critic.calls,1,'T');assert.equal(resolveReviewDecision(audit(false),good),'rejected','B-F');assert.equal(resolveReviewDecision(audit(),{...good,overallScore:83}),'approved_with_notes');const major={...good,overallScore:80,issues:[{id:'generic',severity:'major' as const,domain:'concept',diagnosis:'technically correct but generic',whyItMatters:'weak distinction',evidenceRefs:[],recommendedAction:'target revision',revisionTarget:'creative_route' as const,confidence:.9,observability:'observable_pre_render' as const}]};assert.equal(resolveReviewDecision(audit(),major),'revision_required','H-K');assert.equal(APPROVAL_POLICY.maxRevisionRounds,1,'S-W');assert.equal(OBSERVABILITY.geometry,'observable_pre_render','AF');assert.notEqual(OBSERVABILITY.rasterArtifacts,'observable_pre_render','AD');assert.notEqual(OBSERVABILITY.imageAuthenticity,'observable_pre_render','AE');assert(observabilityGuard(major.issues));assert(validateSeniorCritique(good));assert(!validateSeniorCritique({...good,overallScore:101}));assert.equal(good.schemaVersion,ART_DIRECTOR_REVIEW_SCHEMA_VERSION);assert.equal(good.issues.filter(i=>i.severity==='blocker'||i.severity==='major').length,0,'AL-AM');console.log('Senior Art Director validation passed: approval, blockers, observability, bounded loop and schema.');
