import type {VisualApprovedRenderPackage} from '../post-render-review/index.js';import {EXPORT_SCHEMA_VERSION,type ProductionExportArtifact,type ProductionManifest} from './types.js';
export const buildProductionManifest=(projectId:string,fingerprint:string,artifacts:ProductionExportArtifact[],pkg:VisualApprovedRenderPackage):ProductionManifest=>({schemaVersion:EXPORT_SCHEMA_VERSION,projectId,packageFingerprint:fingerprint,exportedFormats:[...new Set(artifacts.map(artifact=>artifact.format))],scenes:artifacts.filter(artifact=>artifact.format!=='zip').map(artifact=>({filename:artifact.filename,format:artifact.format,width:artifact.width,height:artifact.height,checksum:artifact.checksum,byteSize:artifact.byteSize,formatId:pkg.reviewedDesignPackage.layoutPlan.format.id,placement:pkg.reviewedDesignPackage.layoutPlan.format.id,sceneId:artifact.sceneId,qaStatus:'approved'})),provenance:{reviewSessionId:pkg.reviewSessionId,postRenderReviewSessionId:pkg.postRenderReviewSessionId,renderSessionId:pkg.renderSession.sessionId,versions:pkg.renderSession.sourceVersions},pixelQaScore:pkg.pixelQaSummary.score,visualApprovalStatus:'approved'});
