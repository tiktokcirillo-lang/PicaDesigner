import {calculateRouteBrandDriftRisk,calculateRouteReferenceImitationRisk} from './risks.js';import type {ArtDirectionRoute,CreativeRouteEvaluation} from './types.js';
export const CREATIVE_SCORE_WEIGHTS={communicationFit:.20,brandFit:.18,constraintCompliance:.15,distinctiveness:.12,referenceTransformation:.10,formatFit:.08,productionFeasibility:.06,visualSophistication:.05,antiAiDiscipline:.03,originality:.03} as const;
export const ROUTE_GATES={referenceImitationRisk:.40,brandDriftRisk:.35,constraintCompliance:.70,communicationFit:.60} as const;
export const evaluateCreativeRoute=(route:ArtDirectionRoute,dimensions:Omit<CreativeRouteEvaluation,'routeId'|'routeBrandDriftRisk'|'routeReferenceImitationRisk'|'weightedScore'|'eligible'|'gateFailures'>):CreativeRouteEvaluation=>{const routeBrandDriftRisk=calculateRouteBrandDriftRisk(route),routeReferenceImitationRisk=calculateRouteReferenceImitationRisk(route);const gateFailures=[...(route.declaredHardViolations?.length?['brand_hard_constraint']:[]),...(route.inventedClaims?.length?['invented_claim']:[]),...(routeReferenceImitationRisk>ROUTE_GATES.referenceImitationRisk?['reference_imitation']:[]),...(routeBrandDriftRisk>ROUTE_GATES.brandDriftRisk?['brand_drift']:[]),...(dimensions.constraintCompliance<ROUTE_GATES.constraintCompliance?['constraint_compliance']:[]),...(dimensions.communicationFit<ROUTE_GATES.communicationFit?['communication_fit']:[])];const weightedScore=(Object.keys(CREATIVE_SCORE_WEIGHTS) as Array<keyof typeof CREATIVE_SCORE_WEIGHTS>).reduce((sum,key)=>sum+dimensions[key]*CREATIVE_SCORE_WEIGHTS[key],0);return{routeId:route.id,...dimensions,routeBrandDriftRisk,routeReferenceImitationRisk,weightedScore,eligible:gateFailures.length===0,gateFailures};};
