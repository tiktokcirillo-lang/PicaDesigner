import type {ArtDirectionRoute,CreativeRouteEvaluation} from './types.js';
export const selectBestCreativeRoute=(routes:ArtDirectionRoute[],evaluations:CreativeRouteEvaluation[])=>{const eligible=evaluations.filter(item=>item.eligible).sort((a,b)=>b.weightedScore-a.weightedScore||a.routeBrandDriftRisk-b.routeBrandDriftRisk||a.routeReferenceImitationRisk-b.routeReferenceImitationRisk||b.communicationFit-a.communicationFit||b.distinctiveness-a.distinctiveness);const evaluation=eligible[0];return evaluation?{evaluation,route:routes.find(({id})=>id===evaluation.routeId)}:undefined;};
