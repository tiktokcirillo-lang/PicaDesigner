import type {ArtDirectionRoute} from '../creative-direction/index.js';import type {BrandDNA} from '../brand-intelligence/index.js';import type {CanvasSpec,GridDefinition,SpacingScale} from './types.js';
export const resolveGrid=(canvas:CanvasSpec,route:ArtDirectionRoute,brand:BrandDNA|undefined,density:number,spacing:SpacingScale):GridDefinition=>{const editorial=brand?.compositionCharacter?.editorialness??(route.compositionStrategy.toLowerCase().includes('editorial')?.8:.4);const modular=brand?.compositionCharacter?.modularity??(route.spatialStrategy.rhythm.toLowerCase().includes('modular')?.8:.4);let columns=canvas.orientation==='portrait'?4:8;if(editorial>.65)columns=canvas.orientation==='landscape'?12:canvas.aspectRatio<.7?4:6;else if(modular>.65)columns=canvas.orientation==='landscape'?12:8;else if(density>.7)columns=canvas.orientation==='portrait'?6:8;const baselineUnit=Math.max(2,Math.round(spacing.baseUnit/2));return{kind:editorial>.65?'column':modular>.65?'modular':'hybrid',columns,rows:canvas.orientation==='portrait'?12:8,gutter:spacing.tokens.sm,margin:canvas.safeArea,baseline:{unit:baselineUnit,origin:Math.round(canvas.safeArea.top*canvas.height),snapTolerance:baselineUnit*.35},modularUnits:[spacing.baseUnit,spacing.tokens.sm,spacing.tokens.md]}};
export const snapToBaseline=(value:number,baseline:GridDefinition['baseline'])=>{const snapped=baseline.origin+Math.round((value-baseline.origin)/baseline.unit)*baseline.unit;return Math.abs(snapped-value)<=baseline.snapTolerance?snapped:value};
