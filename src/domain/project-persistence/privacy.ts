const SECRET=/database_url|token|credential|authorization|api[_-]?key|rawprompt|reasoning/i;
export const assertMetadataOnly=(value:unknown,path='root'):void=>{if(value instanceof Uint8Array||value instanceof ArrayBuffer)throw new Error(`Binary data is forbidden in Postgres metadata: ${path}`);if(typeof value==='string'&&/^data:(?:image|application\/(?:pdf|zip));base64,/i.test(value))throw new Error(`Base64 binary is forbidden in Postgres metadata: ${path}`);if(Array.isArray(value)){value.forEach((item,index)=>assertMetadataOnly(item,`${path}[${index}]`));return}if(value&&typeof value==='object')for(const [key,item] of Object.entries(value)){if(SECRET.test(key))throw new Error(`Private field is forbidden in durable state: ${key}`);assertMetadataOnly(item,`${path}.${key}`)}};
